import styled from 'styled-components';

export const StyledFooter = styled.footer`
  margin-inline: auto;
  max-width: 1400px;
  position: relative;
  display: grid;
  gap: 3rem;
  padding: 9.2rem 5rem 0;
`;
