import styled from 'styled-components';

export const StyledStatute = styled.div`
  h1 {
    font-size: 1.5rem;
    padding-bottom: min(1.5rem, 3vw);
  }

  h2 {
    font-size: 1.25rem;
    padding-bottom: min(0.8rem, 2vw);
  }

  h3 {
    font-size: 1rem;
    padding-bottom: min(0.6rem, 1.5vw);
  }

  p,
  li {
    font-size: 0.9rem;
    padding-bottom: 0.3rem;
  }

  .important {
    font-size: 0.9rem;
    font-weight: 600;
    padding-bottom: 0.5rem;
  }
  .end {
    padding-bottom: min(2rem, 4vw);
  }

  ul {
    padding-top: min(0.5rem, 1.5vw);
    padding-left: 0.5rem;
  }
`;
