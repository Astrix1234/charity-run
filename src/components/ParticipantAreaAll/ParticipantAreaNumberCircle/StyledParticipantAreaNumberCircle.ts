import styled from 'styled-components';

export const StyledCircle = styled.div`
  display: grid;
  place-items: center;
  color: var(--light);
  border-radius: 50%;
  background-color: var(--orange);
  width: min(7.73rem, 25vw);
  height: min(7.73rem, 25vw);
  font-size: min(4.5rem, 50vw);
  font-weight: 600;
`;
